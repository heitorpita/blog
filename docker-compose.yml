services:
  db:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: sinapse
      POSTGRES_PASSWORD: sinapse
      POSTGRES_DB: sinapse
    ports:
      - "5433:5432"
    volumes:
      - sinapse_dev_db:/var/lib/postgresql

volumes:
  sinapse_dev_db:
